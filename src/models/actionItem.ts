export interface SidebarItem {
  id: string;
  label: string;
  subtitle?: string;
  icon: string;
  gradientClass: string;
}

export interface AppMenuItem {
  id: string;
  title: string;
  subtitle: string;
  icon: string;
  bgClass: string;
}
